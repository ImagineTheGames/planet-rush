a0-83 — one CI run per push
Run counts before and after, for the three refs that matter.

Repo:   ImagineTheGames/planet-rush
Branch: agent/platform/a0-83-ci-single-run
PR:     #453
Change: 047e7c2  ci(a0-83): one run per push — `push` stops shadowing `pull_request`
Date:   2026-08-18

  BEFORE                                AFTER
  on:                                   on:
    push:                                 push:
      branches: ['**']                      branches: [main]
      tags: ['v*']                          tags: ['v*']
    pull_request:                         pull_request:
                                          workflow_dispatch:

                                        concurrency:
                                          group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
                                          cancel-in-progress: true


================================================================================
(a) AGENT BRANCH WITH AN OPEN PR
================================================================================

BEFORE — every push fired two identical runs, because a branch with an open PR
matches both `push: branches: ['**']` and `pull_request`.

  Population: the 200 most recent CI runs as of 2026-08-18 03:00Z, restricted
  to non-main head branches, grouped by head SHA.

    $ gh run list --workflow ci.yml --limit 200 \
        --json databaseId,event,headBranch,headSha,createdAt --jq \
        '[.[]|select(.headBranch!="main")]|group_by(.headSha)
         |map({n:length})|group_by(.n)|map({runs_per_sha:.[0].n,shas:length})'

      [{"runs_per_sha":1,"shas":16},{"runs_per_sha":2,"shas":81}]

    81 of 97 commits built twice.  (The 16 singletons are pushes made before
    the branch's PR was opened — the same asymmetry this change makes uniform.)

  Event split over the same 200 runs:  119 push  /  81 pull_request.

  The two pairs named in the brief, re-read from the API:

    a0-81  753aeae3   32085980061  push          2026-08-18T00:49:46Z  success
                      32085981387  pull_request  2026-08-18T00:49:47Z  (same second)
    a0-77  245a25f3   32087982679  push          2026-08-18T01:22:30Z
                      32088019964  pull_request  2026-08-18T01:23:07Z  (+37s)

  The cost that is not minutes: the twins are not synchronised, so `gh pr checks`
  reports UNSTABLE until the slower one finishes. #449 and #450 were
  green-but-unmerged for over two hours on that.

AFTER — pushes to this very branch, which has PR #453 open.

  1. Branch created and pushed (commit 047e7c2), BEFORE the PR existed:

     $ git push -u origin agent/platform/a0-83-ci-single-run
     $ gh run list --branch agent/platform/a0-83-ci-single-run

       (no runs)                                          <- 0 runs, as designed

     Under the old `on:` this push alone would have fired one `push` run.

  2. PR #453 opened on the same SHA:

       32094319483  pull_request  047e7c2a  2026-08-18T03:07:18Z   <- 1 run

  3. Trivial commit pushed to the branch with the PR open — the DoD experiment:

