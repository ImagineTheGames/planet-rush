/**
 * evidence/_online-final-entries.mjs — write this round's two manifest entries.
 * OWNER: QA Manager. Run once: `node evidence/_online-final-entries.mjs`.
 *
 * The attestations are long because the findings are specific: each names the
 * file, the line and the artifact a reader can check for themselves. Kept as a
 * script (like `_merge-entry.mjs`) so the manifest edit is reproducible rather
 * than hand-patched JSON.
 */
import { readFileSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const HERE = dirname(fileURLToPath(import.meta.url));
const MANIFEST = join(HERE, 'manifest.json');
const manifest = JSON.parse(readFileSync(MANIFEST, 'utf8'));

const RECONNECT = {
  id: 'online-reconnect',
  title:
    'Reconnect mid-match — the reclaim RE-SEATS and RESUMES on the live fleet, but the WALLET still does not come back there: the gameserver deploy has been RED since 2026-07-29T08:25Z, so #241 is not running. On a fleet built from HEAD the bank and the held cargo DO come back; the upgrade tiers cannot be tested at all, because `upgradeOrder` cannot cross the online wire',
  area: 'online',
  image: 'images/online-final-reconnect-live-landed.json',
  capturedAt: '2026-07-30T15:25:00Z',
  buildSha: '927943a',
  verdict: 'failed',
  attestation: [
    'SELF-WITNESSED, 2026-07-30T14:45Z–15:20Z, against BOTH the deployed Fly fleet and a fleet stood up from the code at HEAD (927943a — the sha the deployed client bundle also reports, `version.json` + the log header). Instruments, all in evidence/ and all runnable: `online-final.live.test.ts` (the shipped src/net client modules over the real internet), `probe-wire-orders.mjs` (hand-built JSON frames on a raw socket), `fleet-head.mjs` (the two SHIPPED server bundles, run here).',

    'THE GATE FAILS ON THE LIVE FLEET, AND THE REASON IS NOT THE NETCODE. Two live runs, both self-witnessed. (a) A run where the reclaim LANDED (evidence/images/online-final-reconnect-live-landed.json, room 84LX): the socket was severed with no goodbye at tick 56, the peer was told over the wire the seat was substituted, the client redialled inside grace and reclaimed — reclaimedSameSeat=true, BOTH peers saw `playerReclaimed`, the match resumed to tick 518 — and the reclaim welcome carried NO `economy` field at all, so the client rebuilt from the seed and the wallet read banked=3 / every tier 0, the spawn defaults. (b) A later run with the wallet built the authoritative way (evidence/images/online-final-reconnect-live.json, 15:11Z): ZERO `economy` frames were received in the whole session, so an authoritative wallet could not even be established to lose.',

    'ROOT CAUSE, NAMED: THE DEPLOYED MATCH SERVER PREDATES THE FIX. `gh run list --workflow=deploy-server.yml` — every "Deploy server (Fly.io)" run since 2026-07-29T08:25:09Z has FAILED (runs 30482064820, 30507870626, 30510772768, 30541430242, 30543630689, 30549466683, 30552080733). The live gameserver `/health` reports uptimeSeconds 109398 (~30.4 h), which lands exactly on that last green deploy. PR #241 landed in `server/` at 2026-07-30T11:09Z and 11:45Z — a day AFTER the newest running image. So `WelcomeMessage.economy` and the `economy` channel exist in the repository and are not running in iad. The build log names the failure in one line: `server/upgrade-router.ts(29,33): error TS2307: Cannot find module \'../allocator/router\'` — `server/Dockerfile` copies package.json, tsconfig.json, src, content and server, and never `allocator/`, while `server/upgrade-router.ts` (added by the machine-pin fix) imports `../allocator/router`. The image\'s own `RUN npx tsc --noEmit` therefore fails and no image is produced. One missing COPY line has frozen the fleet for a day and a half.',

    'THE SAME STALE IMAGE IS WHY THE RECONNECT ITSELF IS A COIN FLIP. `MATCH_ROUTER="fly"` — the arming of the socket-hop machine-pin — is in `fly.gameserver.toml` in the repo and not in the running Machines, so Fly\'s edge lands a ticketed upgrade on the room\'s own Machine only about half the time. Measured with the repo\'s own probe, twice, this session: 5/10 and 5/12 welcome (evidence/images/online-final-wire-pin.txt, `node tests/net/wire-probe.mjs`). A refused join is TERMINAL by design in the shipped client (M10), so a reclaim redial that misses the pin ends the session outright — 4 of my first 5 live reconnect attempts died exactly there, one redial, one `joinError: bad-ticket`, socket closed.',

    'ON THE CODE AT HEAD, THE WALLET DOES COME BACK — TWO THIRDS OF IT. `evidence/fleet-head.mjs` builds and runs the SHIPPED `allocator/dist/allocator.mjs` and `server/dist/match-server.mjs` and wires them over the same authenticated fleet routes a Fly Machine uses; nothing is stubbed. Against it (evidence/images/online-final-reconnect-head.json, room ZT29): the player built a wallet AUTHORITY ITSELF acknowledged on its own `economy` channel — a TURRET bought from the home orbit took banked 3 → 0, and shooting the nearest rock (124 u away) put held ore at 1 — then the socket was severed at tick 334, the peer saw the substitution, a bot flew the ship for 18 s, and the reclaim landed. The reclaim welcome carried `{held: 1, banked: 0, tiers: all 0}` — NOT the spawn default of held 0 / banked 3 — and the returning client stamped exactly that onto the ship it predicts from (cargo 1, banked 0, hull class interceptor), then ran on to tick 1510. Bank: restored. Held cargo: restored. Both were witnessed on the wire, not read off the client\'s own prediction.',

    'THE THIRD THIRD CANNOT BE TESTED, AND THAT IS A SEPARATE DEFECT I FOUND WHILE TESTING THIS ONE: **`upgradeOrder` CANNOT CROSS THE ONLINE WIRE.** `parseActions` (src/net/wire.ts) switches on thrust / aim / fire / build / buildOrder and `default: return null`, and its own comment says one bad action rejects the WHOLE message. `UpgradeOrderAction` is the sixth verb of the `Action` union (src/shared/types.ts) and has no case, so every input tick carrying a ship upgrade is discarded entire by the server. The same validator\'s `BUILD_ITEMS` set is `[turret, shield, repair, bank]` — `\'satellite\'` is in `BuildItem` and not in the set, so a RADAR SATELLITE order is discarded the same way. MEASURED, not read (evidence/probe-wire-orders.mjs → images/online-final-wire-orders.json, one fresh room per order, verdict taken from authority\'s own `economy` frames): a lone `buildOrder:turret` charges banked 3 → 0; `turret + thrust` still charges; `turret + buildOrder:shield` still charges; `turret + buildOrder:repair` still charges; but `turret + upgradeOrder:cargo` charges NOTHING, and `turret + buildOrder:satellite` charges NOTHING — the suspect verb takes the turret down with it. So online you can never buy a ship upgrade or a radar satellite; the client predicts the purchase, your own HUD shows the tier and the ore leaving your bank, and authority never saw the order. (This also retires the previous round\'s method: that instrument established its "before" wallet by buying CARGO tier 1 and reading it back off its own predicted world — a purchase no server ever made.)',

    'VERDICT: FAILED. The reclaim path is healthy on both fleets and the ship comes back on its own seat; on the code at HEAD the banked ore and the held cargo come back too, exactly as authority holds them. But the gate reads "ship AND full economy (cargo/bank/upgrades) restored" on a LIVE match, and on the live fleet no wallet comes back at all, because the gameserver deploy has been failing for a day and a half on a missing `COPY allocator ./allocator` in `server/Dockerfile`. Even once that is fixed, "upgrades" cannot be satisfied until `parseActions` learns `upgradeOrder` (and `satellite`). Two named defects, two named owners (deploy/CI, and Netcode for the wire validator); neither is QA\'s to fix, and neither is flipped green here.',
  ].join('\n\n'),
};

const FEEL = {
  id: 'online-feel',
  title:
    'Online feel at ~400 ms, measured with the game\'s OWN COPY LOG export from a live two-client match — the #244 wiring reaches the screen and ordinary flight sits far inside every audit threshold (worst correction 1.5 u of 4, snap 0, lead 21 t of 32), but the session as exported BREACHES three of them on respawn, and COPY LOG cannot be pressed at all on a phone',
  area: 'online',
  image: 'images/online-feel-pc-host-copylog.png',
  capturedAt: '2026-07-30T15:25:00Z',
  buildSha: '927943a',
  verdict: 'failed',
  attestation: [
    'SELF-WITNESSED, 2026-07-30T15:00Z–15:25Z, on the DEPLOYED client (https://imaginethegames.github.io/planet-rush/, bundle index-DbmsgaaZ.js, build sha 927943a = the repo HEAD) against the live Fly fleet over the real internet. Two real Chromium clients walked the shipped front door with real input — PLAY → CREATE ROOM → the code → the other client PLAY → JOIN ROOM → the code typed key by key on the keypad → the host presses RUSH! — and then flew the match for ~38 s each before pausing and pressing COPY LOG. The capture tool IS the deliverable: no QA-private telemetry read, no debug seam scraped; the numbers below are what the button put on the clipboard (evidence/capture-online-feel.mjs; the exports are committed verbatim as images/online-feel-pc-host-log.json and -guest-log.json).',

    'VISIBLE, AND I LOOKED. images/online-feel-pc-room.png — the online lobby, "ROOM J5LG" in plasma at the top right, MODE · FFA, ORE · SCARCE, eight seats with P1 "YOU ★ VANGUARD · AZURE" and seven named bots at EASY/MEDIUM/HARD, the four hull cards, the four arena cards with The Ring selected and Double Diamond wearing VETERAN, and RUSH! over "1 PLAYING · 7 BOTS". images/online-feel-pc-host-match.png — a live arena: "WAVE 1/5 · Outer Drift", NEXT 1:51, MATCH 0:40, TOTAL 3, HOME 99/100, the local ship labelled YOU at its home planet, four rival ships each with a nameplate and a two-part hull bar (P5 27/55, P2 36/50, one at 38/50), TWO red projectiles caught mid-flight in open space, ore-bearing asteroids, the minimap, and the build watermark "927943a · 14:34Z". images/online-feel-pc-host-copylog.png — the pause overlay (PAUSED / RESUME / SETTINGS / EXIT TO MENU) with the plasma COPY LOG button bottom-right, already pressed and reading LOG COPIED above its own hint "Log copied — paste it into chat." The match clock behind it advanced 0:40 → 1:13 while the overlay was up: online authority is not pausable, and the overlay says so by not stopping it.',

    'THE NUMBERS, QUOTED FROM THE EXPORT. Both logs open "Planet Rush playtest log — build 927943a (2026-07-30T14:34:58.423Z) · session … · desktop 1280x800 · net 4g · planet-rush.playtest-log/1", dropped 0, page errors 0. HOST, 71 finalized seconds over 2 111 reconciles: RTT mean 403 ms / max 785 ms, jitter 38 ms, mean correction 25.394 u, WORST correction 359.862 u, misprediction 0.090, mean lead 21.3 t / peak 24 t, visual snaps 1, resyncs 2. GUEST, 105 seconds over 3 129 reconciles: RTT mean 401 ms / max 770 ms, jitter 40 ms, mean correction 17.977 u, WORST 362.353 u, misprediction 0.060, mean lead 21.1 t / peak 24 t, visual snaps 1, resyncs 0.',

    'AGAINST docs/netcode-audit.md §5: `MAX_MEAN_LEAD_TICKS` 32 → 21.3 / 21.1 PASS. `MAX_PEAK_LEAD_TICKS` 120 → 24 PASS (the lead ratchet #244 bounded is provably gone: 24 t against the 33 t / 59 t the audit measured before the fix). `MAX_MISPREDICTION_RATE` 0.5 → 0.090 / 0.060 PASS. `MAX_CORRECTION_UNITS` 4 u → 359.862 / 362.353 FAIL. `MAX_MEAN_CORRECTION_UNITS` 1.5 u → 25.394 / 17.977 FAIL. `MAX_VISUAL_SNAPS` 0 → 1 on each client FAIL.',

    'WHAT THE THREE FAILURES ARE — because "the gate is breached" and "the gate is breached BY this" are different reports. Every breaching second on both clients falls inside ONE window: six consecutive seconds where the correction pins at EXACTLY 359.862 u (host) / 362.353 u (guest), unchanged to three decimals, with the misprediction rate at 1.0 and one visual snap at the leading edge. Outside that window the host\'s 65 remaining seconds read mean correction 0.286 u, worst 1.473 u, misprediction 0.021, snap 0; the guest\'s 99 remaining seconds read 0.176 u, worst 1.473 u, 0.012, snap 0 — every threshold met with an order of magnitude to spare. I reproduced the window with a fourth instrument that samples the SHIP beside the telemetry (evidence/feel-diagnose.live.test.ts → images/online-feel-diagnose.json, live, 75 s, room U72Z) and it is a DEATH AND RESPAWN: the ship\'s hull falls 16 → 13 → 9 → 6 → 2 while flying at world (1225, 154), and the next sample has it at its home spawn (1981, 1200) with hull restored to 35/35. The correction, 1281.442 u in that run, is that distance. The teleport itself is handled exactly as `prediction.ts` says it should be — over `SNAP_THRESHOLD` (120 u) it is snapped once rather than slid, which is why the snap count is 1 and not 0. What is NOT explained is the tail: for the ~5 seconds AFTER the respawn the instrument keeps charging that same correction once per reconcile (~150 times) with the misprediction rate pinned at 1.0, while the sampled ship sits still at (1980 ± 2, 1200). The ship is stable; the accounting is not. That tail is what drags the mean correction from 0.29 u to 25 u.',

    'THE PHONE HALF OF THE GATE COULD NOT BE MEASURED AT ALL, AND THAT IS THE SHARPEST FINDING HERE: **COPY LOG IS UNREACHABLE ON A PHONE.** The phones walk itself works — three separate live two-client phone matches formed this session (rooms JVSX, 4NFM, U3ZA; iPhone 390x844 host + Pixel 412x915 guest, both held PORTRAIT so every press goes through the landscape-lock remap), and images/online-feel-phones-room.png, -roster.png, -guest-keypad.png and -host-match.png show the rotated lobby with its room code, the roster, the keypad and a live rotated arena (BUILD & UPGRADE, FIRE, TOTAL 3, HOME 100/100, WAVE 1/5 · Outer Drift, watermark 927943a · 14:34Z). But at the pause menu — images/online-feel-phones-host-copylog.png, which I looked at — the overlay is up (PAUSED / RESUME / SETTINGS / EXIT TO MENU, correctly rotated) and there is NO COPY LOG BUTTON ANYWHERE ON THE SCREEN. It is in the DOM, at an on-screen rect (270, 788, 108x44 in a 390x844 viewport). It is simply not rendered. Cause, established by asking the browser rather than the test runner (evidence/probe-copylog-reach.mjs → images/copylog-reach.json, three profiles, offline solo so no fleet variable is in the answer): `document.elementFromPoint` at the button\'s own centre returns `playtest-copy-log-button` on desktop and returns **CANVAS** on both phone profiles. `document.fullscreenElement` is null on desktop and `DIV#app` on both phones — the mobile landscape lock enters ELEMENT fullscreen on the game root (`requestLandscape`, src/platform/orientation.ts), and the Fullscreen API paints only that subtree, while `installCopyLogButton` appends the affordance to `document.body` (deliberately, so it survives a screen the renderer is failing to draw — src/net/playtest-log-button.ts). Outside the subtree, at any z-index, it is neither painted nor hit-tested. So the one affordance built so a phone in a classroom can hand a session back is the one place it cannot be pressed. desktop PRESSABLE=true; phone-portrait-held PRESSABLE=false; phone-landscape PRESSABLE=false.',

    'ALSO NOTED IN PASSING, from reading the exports against their own documentation: the logs carry no local spawn or death entry, though docs/playtest-log.md\'s `match` row lists "local `spawn` / `death`" — `src/net/playtest-log-attach.ts` records only matchStart, playerSubstituted, playerReclaimed and matchEnd. Had a death been in the log, the six-second correction window would have explained itself on the page instead of needing a fourth instrument.',

    'VERDICT: FAILED — and the failure is narrow and worth reading twice, because most of what this gate asks about is in excellent shape. The #244 wiring demonstrably reaches the screen on the deployed build: in 164 of 176 measured seconds across two clients on a ~400 ms wire, the worst correction anywhere is 1.473 u against a 4 u ceiling, the mean is 0.29 u / 0.18 u against 1.5 u, the misprediction rate is 2 % and 1 %, the lead is bounded at 21 t mean and 24 t peak against 32 / 120 — the ratchet is gone — and there is not one visual snap. The COPY LOG pipeline itself works end to end on desktop and is a genuinely good instrument. But the gate is "numbers against the audit\'s thresholds", the session as the button exports it breaches three of the six, and the capture tool cannot be operated at all on one of the two form factors the brief requires. Two defects to hand back: the post-respawn correction tail in the telemetry accounting (Netcode), and the fullscreen-orphaned COPY LOG affordance on mobile (Platform/UI — the button needs to mount inside the fullscreen element, or the lock needs to fullscreen a container that holds both).',
  ].join('\n\n'),
};

for (const entry of [RECONNECT, FEEL]) {
  const at = manifest.findIndex((e) => e.id === entry.id);
  if (at >= 0) manifest[at] = entry;
  else manifest.push(entry);
  console.log(`${at >= 0 ? 'replaced' : 'added'} ${entry.id} (${entry.verdict})`);
}
writeFileSync(MANIFEST, `${JSON.stringify(manifest, null, 2)}\n`);
console.log(`manifest: ${manifest.length} entries`);
