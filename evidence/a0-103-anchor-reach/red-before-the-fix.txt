[pool-size] worker cap 2: cgroup quota 6 cores / LANES 3 = 2 workers (host claims 6)

 RUN  v2.1.8 /lanes/lane-1

 ❯ src/ui/minimap.test.ts (88 tests | 1 failed) 38ms
   × a0-103 — an element declared to an anchor reaches that anchor > a bottom-right element actually reaches the bottom-right 14ms
     → 
iphone/portrait 390×844 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:195.0, y:769.0, w:63.0, h:63.0} in frame {x:0.0, y:0.0, w:390.0, h:844.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone/portrait 390×844 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:195.0, y:769.0, w:63.0, h:63.0} in frame {x:0.0, y:0.0, w:390.0, h:844.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone/landscape 844×390 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:632.0, y:298.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:844.0, h:390.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone/landscape 844×390 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:632.0, y:298.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:844.0, h:390.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
qa-phone/landscape 798×384 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:586.0, y:292.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:798.0, h:384.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
qa-phone/landscape 798×384 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:586.0, y:292.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:798.0, h:384.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/portrait 412×915 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:206.0, y:829.0, w:74.0, h:74.0} in frame {x:0.0, y:0.0, w:412.0, h:915.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/portrait 412×915 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:206.0, y:829.0, w:74.0, h:74.0} in frame {x:0.0, y:0.0, w:412.0, h:915.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/landscape 915×412 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:703.0, y:320.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:915.0, h:412.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/landscape 915×412 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:703.0, y:320.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:915.0, h:412.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone-se/portrait 375×667 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:187.5, y:599.5, w:55.5, h:55.5} in frame {x:0.0, y:0.0, w:375.0, h:667.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone-se/portrait 375×667 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:187.5, y:599.5, w:55.5, h:55.5} in frame {x:0.0, y:0.0, w:375.0, h:667.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/portrait 320×568 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:160.0, y:528.0, w:28.0, h:28.0} in frame {x:0.0, y:0.0, w:320.0, h:568.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/portrait 320×568 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:160.0, y:528.0, w:28.0, h:28.0} in frame {x:0.0, y:0.0, w:320.0, h:568.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/landscape 568×320 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:356.0, y:228.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:568.0, h:320.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/landscape 568×320 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:356.0, y:228.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:568.0, h:320.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
: expected [ …(16) ] to deeply equal []

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  src/ui/minimap.test.ts > a0-103 — an element declared to an anchor reaches that anchor > a bottom-right element actually reaches the bottom-right
AssertionError: 
iphone/portrait 390×844 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:195.0, y:769.0, w:63.0, h:63.0} in frame {x:0.0, y:0.0, w:390.0, h:844.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone/portrait 390×844 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:195.0, y:769.0, w:63.0, h:63.0} in frame {x:0.0, y:0.0, w:390.0, h:844.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone/landscape 844×390 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:632.0, y:298.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:844.0, h:390.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone/landscape 844×390 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:632.0, y:298.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:844.0, h:390.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
qa-phone/landscape 798×384 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:586.0, y:292.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:798.0, h:384.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
qa-phone/landscape 798×384 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:586.0, y:292.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:798.0, h:384.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/portrait 412×915 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:206.0, y:829.0, w:74.0, h:74.0} in frame {x:0.0, y:0.0, w:412.0, h:915.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/portrait 412×915 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:206.0, y:829.0, w:74.0, h:74.0} in frame {x:0.0, y:0.0, w:412.0, h:915.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/landscape 915×412 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:703.0, y:320.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:915.0, h:412.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
pixel/landscape 915×412 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:703.0, y:320.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:915.0, h:412.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone-se/portrait 375×667 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:187.5, y:599.5, w:55.5, h:55.5} in frame {x:0.0, y:0.0, w:375.0, h:667.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
iphone-se/portrait 375×667 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:187.5, y:599.5, w:55.5, h:55.5} in frame {x:0.0, y:0.0, w:375.0, h:667.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/portrait 320×568 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:160.0, y:528.0, w:28.0, h:28.0} in frame {x:0.0, y:0.0, w:320.0, h:568.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/portrait 320×568 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:160.0, y:528.0, w:28.0, h:28.0} in frame {x:0.0, y:0.0, w:320.0, h:568.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/landscape 568×320 [tap/auto-aim] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:356.0, y:228.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:568.0, h:320.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
small/landscape 568×320 [sticks/manual] — "minimap" declares "bottom-right" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:356.0, y:228.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:568.0, h:320.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.
: expected [ …(16) ] to deeply equal []

- Expected
+ Received

- Array []
+ Array [
+   "iphone/portrait 390×844 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:195.0, y:769.0, w:63.0, h:63.0} in frame {x:0.0, y:0.0, w:390.0, h:844.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "iphone/portrait 390×844 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:195.0, y:769.0, w:63.0, h:63.0} in frame {x:0.0, y:0.0, w:390.0, h:844.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "iphone/landscape 844×390 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:632.0, y:298.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:844.0, h:390.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "iphone/landscape 844×390 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:632.0, y:298.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:844.0, h:390.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "qa-phone/landscape 798×384 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:586.0, y:292.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:798.0, h:384.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "qa-phone/landscape 798×384 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:586.0, y:292.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:798.0, h:384.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "pixel/portrait 412×915 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:206.0, y:829.0, w:74.0, h:74.0} in frame {x:0.0, y:0.0, w:412.0, h:915.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "pixel/portrait 412×915 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:206.0, y:829.0, w:74.0, h:74.0} in frame {x:0.0, y:0.0, w:412.0, h:915.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "pixel/landscape 915×412 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:703.0, y:320.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:915.0, h:412.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "pixel/landscape 915×412 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:703.0, y:320.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:915.0, h:412.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "iphone-se/portrait 375×667 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:187.5, y:599.5, w:55.5, h:55.5} in frame {x:0.0, y:0.0, w:375.0, h:667.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "iphone-se/portrait 375×667 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:187.5, y:599.5, w:55.5, h:55.5} in frame {x:0.0, y:0.0, w:375.0, h:667.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "small/portrait 320×568 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:160.0, y:528.0, w:28.0, h:28.0} in frame {x:0.0, y:0.0, w:320.0, h:568.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "small/portrait 320×568 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:160.0, y:528.0, w:28.0, h:28.0} in frame {x:0.0, y:0.0, w:320.0, h:568.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "small/landscape 568×320 [tap/auto-aim] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:356.0, y:228.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:568.0, h:320.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+   "small/landscape 568×320 [sticks/manual] — \"minimap\" declares \"bottom-right\" but stops 132.0 px short of its right edge; at most 12.0 px is allowed. bounds {x:356.0, y:228.0, w:80.0, h:80.0} in frame {x:0.0, y:0.0, w:568.0, h:320.0} — reserved because: The hold-to-FIRE button owns the extreme bottom-right corner on the frames it is drawn (GDD §2.4; `@platform/touch-visuals`), and the bottom-right band is too short to stack the map above it, so the square sits left of the fire column — `./minimap` `MINIMAP_FIRE_COLUMN`. **This row is a0-103.** The column used to be taken on every touch frame off `isTouch` alone, including under Tap Commander, which draws no FIRE: a 132 px gap on a 798x384 phone with nothing in it. It is reserved when the button is there and not otherwise, and the registry now says which.",
+ ]

 ❯ src/ui/minimap.test.ts:1551:52
    1549|       }
    1550|     }
    1551|     expect(failures, `\n${failures.join('\n')}\n`).toEqual([]);
       |                                                    ^
    1552|   });
    1553| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯

 Test Files  1 failed (1)
      Tests  1 failed | 87 passed (88)
   Start at  05:09:15
   Duration  7.05s (transform 1.66s, setup 0ms, collect 5.64s, tests 38ms, environment 0ms, prepare 897ms)

