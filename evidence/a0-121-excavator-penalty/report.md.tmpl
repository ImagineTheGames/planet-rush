# a0-121 — the penalty was charged in full; the number the design gave it is the thing that is wrong

> **HEADLINE_LINE**

**the ship-class and cast contests re-run on a0-112's own seeds, branch
agent/gameplay/a0-121-charge-the-excavator-its-penalty**

Tables generated by `evidence/a0-121-excavator-penalty/render.ts` from the section
artifacts in `tests/reports/a0-121-data/`, using `harness/mirrors`' own helpers so
a table here and the same table in `a0-112-balance.md` are read the same way.
**Every before/after pair is the same draw** — the renderer runs a0-117's own
`assertSameSeeds` on each pair and throws rather than print two columns that are
not, because "re-measured on the **same seeds**" is the whole evidentiary value of
an after-number. The seeds are a0-112's, named in §1. The static tables (§2, §3)
are arithmetic over `SHIP_STATS` itself, so they cannot drift from the class table
they are about. Only the readings are hand-written, and they are marked.

---

## Targets at a glance

{{VERDICT}}

---

## 1 · What was run

| section | lineup | seeds | rotations | matches |
|---|---|---|---|---|
| `class` | ship-class contest — behaviour = `sable`, four hulls, 4 rotations | 64 | 4 | 256 |
| `roster` | the shipped cast, 7 rotations (every character holds every seat once) | 32 | 7 | 224 |

Seeds are `1 … 64` (class) and `1 … 32` (roster) — a0-112 §1's own draw, so the
shipped-tree column of every table below is directly comparable to the number
`a0-112-balance.md` published. Ladder and ablation rows (§4, §5, §6) are 16 seeds
× 4 rotations = **64 matches** each, the same denominator a0-117's lever table
used; ±1 SE at that size is about **6 points**, and no reading here leans on a
difference smaller than that.

Reproduce any row:

```
evidence/a0-121-excavator-penalty/run.sh <label> <section> <seeds> [sed-expr …]
npx vite-node evidence/a0-121-excavator-penalty/render.ts
```

---

## 2 · What GDD §2.11 says the excavator pays

Quoted, in full, because this brief was written on the belief that the simulation
ignored it:

> | Class (hull) | Role · Speed / Accel / Turn / Hull / Power / Cargo |
> |---|---|
> | Excavator (Pincer) | Mining engine, close bruiser · **90% / 100% / 80%** / 55 / 13 / 2 |
>
> […] Baselines are opening hypotheses (**TUNABLE**), relative to the Vanguard
>
> The intended rock-paper-scissors around the triangle: the Interceptor catches
> miners in the open but melts against turrets; **the Excavator out-earns everyone
> but can't run** […] QA gains a third measurable target: **no class exceeds a 55%
> win rate** in mirror-vs-field harness runs.

So the stated penalty is exactly two numbers — **top speed 90%** and **turn rate
80%**, both relative to the Vanguard — plus one sentence of intent, *"out-earns
everyone but can't run."*

**And §2.11 is vague about the size of it, in the one way that matters.** It states
the penalty as *ratios* and never states the absolutes those ratios multiply.
`BASE_SPEED` (260 u/s), `BASE_ACCEL` (900 u/s²) and `BASE_TURN_RATE`
({{BASE_TURN_RATE}} rad/s) are not §2.11 values — they are derived-physics
constants that §2.8 does not carry either, flagged TUNABLE and handed over with no
target attached to them. A ratio is only a penalty if the quantity it scales is
scarce. §2.11 fixes the ratio and leaves the scarcity to a constant it never
mentions, and **that gap is this whole brief.** It is reported here as the finding
the brief asked for: the design cannot be implemented precisely as written,
because as written it does not say how much 80% is supposed to hurt.

---

## 3 · What the simulation charged — before

### 3.1 · The design column beside the simulation column

{{COLUMNS}}

### 3.2 · Cell by cell: does the sim transcribe §2.11?

{{TRANSCRIPTION}}

{{DRIFT}}

### 3.3 · §2.11's rock-paper-scissors, priced

Time to kill, stock hulls, no upgrades — `hull HP ÷ weapon dps`, both read out of
the class table:

{{DUEL}}

{{SCORE}}

---

## 4 · What the penalty is worth, measured

Each row deletes or changes **one cell** of the class table and re-runs the same
64-match ship-class contest. The shipped tree is the first row.

{{ABLATION}}

---

## 5 · The `BASE_TURN_RATE` ladder — the dial that moves the whole cast

Every hull's agility, scaled together. §2.11's 140/100/80/85 spread is untouched
in every row.

{{LADDER_BASE}}

---

## 6 · The `turnMul` ladder — the excavator's nose, and nothing else

The other three hulls keep `BASE_TURN_RATE` × their own §2.11 multiplier in every
row below. Nothing about the cast moves.

{{LADDER_HULL}}

---

## 7 · Before and after, on the same seeds

### 7.1 · The ship-class contest — 64 seeds × 4 rotations

{{CLASS_COMPARE}}

### 7.2 · The cast contest, cut by silhouette — 32 seeds × 7 rotations

{{ROSTER_CLASS_COMPARE}}

### 7.3 · The cast contest, cut by character

{{CHARACTER_COMPARE}}

### 7.4 · Match length — the target a hull nerf can trade away

{{LENGTHS}}

---

## 8 · Reading

READING_BODY

---

*a0-121 — the excavator's penalty exists in the GDD and nowhere else — Gameplay
Engineer.*
