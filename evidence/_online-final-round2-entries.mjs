#!/usr/bin/env node
/**
 * evidence/_online-final-round2-entries.mjs — rewrite the two online-final gate
 * entries after the 2026-07-30 16:00–18:00Z re-verification, and add the RUSH!
 * regression found while trying to capture them. OWNER: QA Manager.
 *
 *   node evidence/_online-final-round2-entries.mjs
 */
import { readFileSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const HERE = dirname(fileURLToPath(import.meta.url));
const MANIFEST = join(HERE, 'manifest.json');
const SHA = '6888456';
const AT = '2026-07-30T17:40:00Z';

const entries = {
  'online-reconnect': {
    title:
      'Reconnect mid-match on a LIVE fleet that finally runs the fix: the seat, the ship, the BANK and the HELD CARGO all come back, witnessed on the wire — the deploy that had been red since 2026-07-29 is green and the wallet is really there. The UPGRADES third still cannot be shown at all, because no upgrade tier can be made to exist online: `parseActions` still rejects `upgradeOrder`, and the stand-in that COULD buy one never earns the ore inside the 60 s grace',
    image: 'images/online-final-reconnect-freshfleet.json',
    verdict: 'failed',
    attestation: `SELF-WITNESSED, 2026-07-30T16:18Z–17:00Z, against the DEPLOYED Fly fleet over the real internet, using the SHIPPED src/net client modules (evidence/online-final.live.test.ts, evidence/online-upgrade-reclaim.live.test.ts). Transcripts committed verbatim.

WHAT CHANGED SINCE THE LAST ROUND, AND IT IS THE WHOLE STORY OF THE FIRST TWO THIRDS. The blocker this gate was failed on is GONE. \`server/Dockerfile\` now carries \`COPY allocator ./allocator\` (line 48, landed on main as c09b94f/#248), the "Deploy server (Fly.io)" run 30560779741 went GREEN at 2026-07-30T16:17Z — the first green server deploy since 2026-07-29T08:25Z — and the live gameserver's \`/health\` went from uptime 114737 s (31.9 h) to 22 s while I watched. The deployed CLIENT bundle also rebuilt: \`version.json\` reports sha 6888456 built 2026-07-30T16:21Z. For the first time in this milestone the client, the server and the repo are the same commit.

THE FIRST TWO THIRDS NOW PASS ON THE LIVE FLEET, NOT MERELY ON A HEAD FLEET. Room AZT2, 16:22Z (images/online-final-reconnect-freshfleet.json). The player bought a TURRET from her home orbit — authority's own \`economy\` channel acknowledged banked 3 → 0 — then flew out and shot the nearest rock at 112 u until that same channel reported held 1. So the "before" wallet is authority's word, not the client's prediction. The socket was then severed with no goodbye at tick 432; the peer received \`playerSubstituted\`; a bot flew the ship for 18 s; the client redialled inside grace and reclaimed on the FIRST dial, with zero \`joinError\` frames past the drop (the machine-pin is armed now — last round it was a coin flip and 4 of 5 attempts died on a refused join). The reclaim welcome frame carried \`economy: {held: 0, banked: 2, tiers: all 0}\` — NOT the spawn default of held 0 / banked 3 — the original lobby welcome carried no \`economy\` at all (correct: there was no ship yet), and the shipped client stamped exactly that wallet onto the ship it predicts from (banked 2, cargo 0, hull class interceptor), running on to tick 1612. Same seat, socket open, match resumed. BANK: RESTORED, on the wire, on the live fleet. The wallet had also MOVED while she was away — banked 2 is what the stand-in banked, not the 0 she left — which is the stronger reading of GDD §4.2: what comes back is what authority holds now.

THE UPGRADES THIRD IS STILL UNPROVABLE, AND I SPENT THE ROUND ESTABLISHING WHY RATHER THAN ASSERTING IT. Two independent routes to a non-zero tier, both closed:

(1) THE PLAYER CANNOT BUY ONE. \`parseActions\` (src/net/wire.ts:317-360) switches on thrust/aim/fire/build/buildOrder with \`default: return null\`, and one bad action rejects the whole message — so every input tick carrying \`upgradeOrder\` is discarded entire. \`BUILD_ITEMS\` is still \`[turret, shield, repair, bank]\`, so \`satellite\` is discarded the same way. Unchanged since last round; measured then in evidence/probe-wire-orders.mjs. Tiers are written in exactly one place in the sim (\`src/sim/buildings.ts:486\`, reached only from the \`upgradeOrder\` case in step.ts:153), so no other verb can produce one.

(2) THE STAND-IN COULD BUY ONE, AND DOES NOT. Bots run INSIDE authority's sim and never cross \`parseActions\`, so the substitute bot seated on a dropped seat (server/room.ts:432, a Medium character) can issue the \`upgradeOrder\` the wire refuses. I built an instrument to make it do so (evidence/online-upgrade-reclaim.live.test.ts) and steered by the two shipped rules that govern it: the ENDOWMENT — "a stand-in never spends the ore it inherited" (src/bots/tree.ts:229, behaviors.ts:353) — and the per-rung affordability guards in \`mediumSpendPlan\` (src/bots/medium.ts), where turret costs 3, shield 5, and the cheapest upgrade rung is CARGO tier 1 at 2. Four configurations, nine live substitution windows in all: dropped broke (endowment 0, three attempts — the bot banked 2.0, 2.0 and 1.9 and could not clear the 2-ore rung); with the turret rung pre-built by the player and ~2 ore left behind so any ore mined clears both bars (three attempts); with the home rocks left unmined for it (one); and parked directly on the asteroid nearest its own station (four, instrumented). NOT ONE PURCHASE. From the fourth configuration I stopped guessing and watched the shift from the PEER's seat, which still receives Alice's ship in every snapshot: across four 52–55 s windows the stand-in flew 1 396–8 967 units of path, and in every single one \`maxCargoSeen\` was 0.00 and \`everDied\` was true with \`minHullSeen\` 0 — two of the four ended parked at the home spawn point. It is flying and fighting and dying in an 8-way FFA (the room fills every empty seat with a bot — server/room.ts:500-506, no way to ask for fewer), and it never holds a single ore, so it never earns above its endowment, so it never buys anything. The wallet came back byte-identical at reclaim in every one of those runs.

So "upgrades restored" cannot be demonstrated by any route available today, and a round-trip of \`tiers: all 0 → all 0\` is not evidence of restoration; it is evidence of nothing. What IS shown is that the \`tiers\` field is carried on the reclaim welcome and stamped onto the ship — the mechanism is present and correct — but it can only ever carry zeros.

VERDICT: FAILED, and much closer than it was. Ship, seat, bank and held cargo are all restored live and witnessed on the wire; the deploy blocker that failed this gate last round is cleared. The single remaining reason is one line of validator: until \`parseActions\` learns \`upgradeOrder\` (and \`BUILD_ITEMS\` learns \`satellite\`), a ship upgrade cannot be bought over the online wire, cannot exist on authority, and therefore cannot be shown to survive a reclaim. OWNER: Netcode. Not QA's to fix, and not flipped green here. Re-run \`node evidence/recheck-online-blockers.mjs\` before spending another capture round: it reports 2 of 5 blockers now CLEAR and this one still BLOCKED.`,
  },

  'online-feel': {
    title:
      'Online feel on a fully fresh stack (client AND server at 6888456): the #244 wiring holds beautifully — a guest who never died passed ALL SIX audit thresholds over 105 s and 3 224 reconciles at ~535 ms RTT — but the host of that same match died once and failed exactly three, in a 6-second window, and the front door itself is now broken: the connect-trace panel is painted over the desktop lobby’s RUSH! button, so an online match cannot be started with a mouse at all',
    image: 'images/online-feel-pc-host-copylog.png',
    verdict: 'failed',
    attestation: `SELF-WITNESSED, 2026-07-30T17:20Z–17:35Z, on the DEPLOYED client (https://imaginethegames.github.io/planet-rush/, build sha 6888456 built 16:21Z) against the freshly-deployed live Fly fleet, over the real internet. Both halves of the stack are HEAD for the first time this milestone. The capture tool is the game's own COPY LOG button (evidence/capture-online-feel.mjs); the exports are committed verbatim as images/online-feel-pc-host-log.json and -guest-log.json and scored by evidence/score-feel-log.mjs → images/online-feel-score.json.

THE FRONT DOOR IS BROKEN ON DESKTOP, AND THAT IS THE FIRST FINDING. The host walks PLAY → CREATE ROOM, the guest walks PLAY → JOIN ROOM and types the code on the real keypad — all of that works, every press landing on the point the client itself reports drawing the control at. Then the host presses RUSH! and NOTHING HAPPENS. I looked at the screen: images/rush-reach-desktop-online.png is the online lobby (ROOM PA4C in plasma top right, MODE · FFA, ORE · SCARCE, eight seats with P1 "YOU ★ VANGUARD · AZURE" and seven named bots at EASY/MEDIUM/HARD, the four hull cards, the four arena cards with The Ring selected) — and THERE IS NO RUSH! BUTTON ANYWHERE ON IT. In its place, bottom-centre, sits a dark panel reading "· ALLOCATING ROOM… · ROOM PA4C · TICKET SIGNED · DIALING MACHINE 0800d5b6… · JOINED · SEAT 1".

CAUSE, ESTABLISHED BY ASKING THE BROWSER (evidence/probe-rush-reach.mjs → images/rush-reach.json, three profiles): \`document.elementFromPoint\` at the RUSH! control's own centre (640, 740) returns **LI** on desktop-online and **CANVAS** on desktop-offline. The panel is \`pr-connect-trace\`, added by fafff19 ("the connecting screen says what it is doing"). \`hideConnectTrace()\` sets \`hidden = true\` on it — but its own stylesheet says \`#pr-connect-trace{position:fixed;z-index:2147483646;…display:flex;…}\` (src/net/connect-trace-view.ts:104), and an id rule with \`display:flex\` beats the user agent's \`[hidden]{display:none}\`. So the element stays laid out, painted and hit-testable forever: computed \`display: flex\`, \`pointer-events: auto\`, rect (400, 673) 480×111 in a 1280×800 view — squarely over the RUSH! rect at (500, 712) 280×56. MEASURED, three ways: PRESSABLE=false on desktop-online; PRESSABLE=true on desktop-offline, where the panel is never built and the same press at the same point starts a match (images/rush-reach-desktop-offline.png shows the resulting arena, WAVE 1/5 · Outer Drift, MATCH 0:01); PRESSABLE=true on phone-online, where the panel lands at (16, 727) and the lobby draws RUSH! higher at (64, 422), so it misses. The lobby's own \`rush()\` seam starts the match instantly, so the client→authority path is healthy — this is purely the panel eating the press. And it never goes away: I looked at images/online-feel-pc-guest-match.png and images/online-feel-pc-host-copylog.png, and the same four-line panel is still painted over the LIVE ARENA on both clients and over the pause menu's EXIT TO MENU button.

So the measurement below had to be taken with the lobby's \`rush()\` seam, and the instrument records that it had to (\`rushViaSeam: true\`, plus images/online-feel-pc-rush-swallowed.png). A run that needs the seam is not evidence that the menu flow works — it is evidence that it does not, plus a measurement taken anyway.

THE NUMBERS, QUOTED FROM THE EXPORT, AND THEY ARE THE BEST THIS GATE HAS SEEN. Both logs open "Planet Rush playtest log — build 6888456 (2026-07-30T16:21:20.031Z) · … · desktop 1280x800 · net 4g · planet-rush.playtest-log/1", dropped 0.

  GUEST — 105 finalized seconds, 3 224 reconciles, RTT mean 535 ms / max 3 401 ms, jitter 55 ms:
    worst correction 1.702 u (≤ 4) PASS · mean correction 0.176 u (≤ 1.5) PASS · visual snaps 0 (= 0) PASS ·
    mean lead 22.8 t (≤ 32) PASS · peak lead 27 t (≤ 120) PASS · misprediction 0.024 (≤ 0.5) PASS  → SIX OF SIX.

  HOST — 71 finalized seconds, 2 169 reconciles, RTT mean 542 ms / max 2 335 ms, jitter 64 ms:
    worst correction 315.221 u FAIL · mean correction 21.980 u FAIL · visual snaps 1 FAIL ·
    mean lead 23.8 t PASS · peak lead 44 t PASS · misprediction 0.102 PASS  → three of six.

THE TWO CLIENTS WERE IN THE SAME MATCH, ON THE SAME WIRE, ON THE SAME BUILD. The only difference between six-of-six and three-of-six is that the host died and the guest did not — which makes this round a controlled comparison rather than an anecdote. Every breaching second on the host falls inside ONE six-second window (at 55.4 s, 56.8 s, 57.7 s, 58.8 s, 59.7 s, 60.3 s) where the correction pins at 315.221 / 314.606 u and the misprediction rate climbs to 1.0. Outside that window the host's remaining 65 seconds read mean correction 0.243 u, worst 1.725 u, snaps 0, misprediction 0.032, lead 23.7 t — SIX OF SIX, the same as the guest. The 315 u is a death-site→home distance; the mechanism was measured directly last round (evidence/respawn-tail.live.test.ts → images/online-feel-respawn-tail.json: 150 consecutive reconciles, one distinct error value, checkpoint frozen at home and authority frozen at the death site, \`histHit\` true and \`resynced\` false throughout) and the code it turns on is UNCHANGED — \`ShipSnap\` still carries no \`respawnTimer\` and \`applySnapshot\` still never restores one, so the client resurrects the local ship the instant authority reports it dead while authority holds it dead for RESPAWN_S = 5 s.

AND THE LOG NOW CORROBORATES THAT MECHANISM ON ITS OWN PAGE. \`playtest-log-attach.ts\` records a \`death\` on every local liveness transition (src/net/playtest-log-attach.ts:201-212) — the gap I reported last round has been closed. Yet the host's log records NO death during the 6-second breach window; the only \`death\` it carries is at 68.78 s, stamped in the same millisecond as \`eliminated\`. That is exactly what the defect predicts: during an ordinary death the client's own predicted ship never STAYS dead long enough for the liveness watcher to see it, so nothing is logged while the correction is charged 150 times; once the player is eliminated the ship cannot respawn, the transition finally persists, and the log finally notices. The instrument agreeing with the mechanism from a completely different direction is the strongest evidence this round produced.

VISIBLE, AND I LOOKED. images/online-feel-pc-guest-match.png — a live online arena from the guest's seat: "WAVE 1/5 · Outer Drift", NEXT 1:50, MATCH 0:41, TOTAL 3, HOME 100/100, the guest's ship labelled YOU at its teal-ringed home planet, a rival nameplated P4 with a two-part hull bar reading 22/35, a second station (P3) at bottom left, ore-bearing asteroids and loose ore chunks, the minimap bottom right, build watermark "6888456 · 16:21Z" — and the connect-trace panel still painted across the bottom centre of the play field. images/online-feel-pc-host-copylog.png — the pause overlay (PAUSED / RESUME / SETTINGS / EXIT TO MENU) with the plasma COPY LOG button bottom-right already pressed and reading "LOG COPIED" above its hint "Log copied — paste it into chat", TOTAL 0 and HOME 0/100 behind it (this is the host after elimination), and the same stale panel overlapping EXIT TO MENU.

THE PHONE HALF IS UNCHANGED AND STILL BLOCKS THIS GATE. \`installCopyLogButton\` still mounts into \`document.body\` (src/net/playtest-log-button.ts:373) while the landscape lock puts \`DIV#app\` into element fullscreen, so the affordance is outside the painted subtree and cannot be pressed on a phone — established last round with \`document.elementFromPoint\` returning CANVAS at the button's own centre on both phone profiles (images/copylog-reach.json), and \`recheck-online-blockers.mjs\` confirms the source is unchanged today. So: the form factor that CAN press RUSH! cannot export a log, and the form factor that CAN export a log cannot press RUSH!. Between the two defects there is no form factor on which this gate can be captured end-to-end through the shipped UI.

VERDICT: FAILED — and, as last round, most of what the gate asks about is in excellent shape. On a ~535 ms wire with 3 401 ms worst-case spikes, ordinary online flight is nowhere near the audit's limits: 105 seconds and 3 224 reconciles of it passed every single threshold, and the host's own 65 non-breaching seconds did too. The lead ratchet #244 bounded is provably still gone (23–24 t mean, 27–44 t peak against 32 / 120). THREE defects to hand back, none of them QA's: the connect-trace panel painted over the desktop lobby's RUSH! button and never dismissed, which stops an online match being started with a mouse at all (OWNER: Netcode/UI — the panel needs \`pointer-events:none\` when hidden, or a \`#pr-connect-trace[hidden]{display:none}\` rule, and it should not outlive the lobby); the post-respawn divergence that breaches three thresholds after every death (OWNER: Netcode — stream \`respawnTimer\`, or hold the local ship dead until authority revives it); and the fullscreen-orphaned COPY LOG affordance on mobile (OWNER: Platform/UI).`,
  },

  'online-rush-swallowed': {
    title:
      'The desktop online lobby has NO pressable RUSH! button: the connect-trace panel is painted over it and never dismissed, so an online match cannot be started with a mouse — offline is unaffected, and the same panel goes on to sit over the live arena and the pause menu',
    area: 'online',
    image: 'images/rush-reach-desktop-online.png',
    verdict: 'failed',
    attestation: `SELF-WITNESSED, 2026-07-30T17:05Z–17:20Z, on the DEPLOYED client (build sha 6888456, built 2026-07-30T16:21Z) at 1280×800. Instrument: evidence/probe-rush-reach.mjs (three profiles, output committed as images/rush-reach.json).

WHAT IS ON THE SCREEN. images/rush-reach-desktop-online.png is the online lobby after PLAY → CREATE ROOM: "ROOM PA4C" in plasma at the top right, MODE · FFA and ORE · SCARCE, eight seats — P1 "YOU ★ VANGUARD · AZURE" then Rusty, Bolt, Foreman, Patch, Vulture, Warden and Sable at EASY/EASY/MEDIUM/MEDIUM/HARD/HARD/HARD, each marked OPEN — the four hull cards (INTERCEPTOR "Quadfin", VANGUARD "Anvil" selected, EXCAVATOR "Pincer", HAULER "Hammerhead") with their descriptions, and the four arena cards (The Ring selected, The Compass, The Oval, Double Diamond wearing VETERAN). THERE IS NO RUSH! BUTTON ON THE SCREEN. Where it should be — the lobby reports drawing it at logical (500, 712) 280×56 — there is instead a dark rounded panel listing "· ALLOCATING ROOM… · ROOM PA4C · TICKET SIGNED · DIALING MACHINE 0800d5b6… · JOINED · SEAT 1", overlapping the arena cards above it.

WHAT IT DOES. A real mouse click at the button's own reported centre (640, 740) does nothing: \`__lobby.counting\` stays false and \`__mainMenu.matchStarted\` stays false, checked for 60 s. A deliberate move-settle-down-hold-up press does nothing. A scan of eight points down the button's column — y = 690, 712, 726, 740, 754, 766, 780, 800 — starts nothing at any of them. The same press mechanism actuates PLAY, CREATE ROOM, JOIN ROOM, every keypad digit and SUBMIT on the way in, so clicks reach the canvas perfectly well everywhere else.

WHY. \`document.elementFromPoint(640, 740)\` answers **LI** — a list item inside \`#pr-connect-trace\` — where the offline lobby answers **CANVAS**. The panel was added by fafff19 to narrate connecting. \`hideConnectTrace()\` sets \`hidden = true\`, and the element's own stylesheet then overrides it: \`#pr-connect-trace{position:fixed;z-index:2147483646;…display:flex;…}\` (src/net/connect-trace-view.ts:104) is an id rule, so it beats the user agent's \`[hidden]{display:none}\`. Measured computed style with \`hidden === true\`: \`display: flex\`, \`pointer-events: auto\`, z-index 2147483646, bounding rect (400, 673) 480×111 — which contains the whole RUSH! rect. The panel is therefore still laid out, still painted and still first in the hit test, permanently.

THE CONTROLS THAT PROVE IT IS THIS AND NOT SOMETHING ELSE. desktop-OFFLINE (the SOLO door, which never builds the panel): element at the same point is CANVAS, the same click STARTS THE MATCH — images/rush-reach-desktop-offline.png is the resulting arena, "WAVE 1/5 · Outer Drift", MATCH 0:01, TOTAL 3, HOME 100/100, the ship labelled YOU at its planet, the control hints "WASD Thrust · Mouse Aim · Left mouse Fire / Mine · E Build & Upgrade", watermark "6888456 · 16:21Z". phone-ONLINE (390×844): the panel is built and lands at (16, 727) 359×101, but a phone lobby draws RUSH! higher, at (64, 422), so the two do not overlap — element CANVAS, PRESSABLE true. And the lobby's own \`rush()\` seam starts the online match on both clients within ~8 s, so nothing is wrong between the client and authority. The defect is exactly one stale DOM panel in exactly one place.

IT DOES NOT STOP AT THE LOBBY. The panel is never dismissed for the rest of the session: I looked at images/online-feel-pc-guest-match.png, where it is painted across the bottom centre of a LIVE ARENA, and images/online-feel-pc-host-copylog.png, where it overlaps the pause menu's EXIT TO MENU button. Anything the game draws under that 480×111 rectangle is both obscured and unclickable.

VERDICT: FAILED. On the deployed build a player on a desktop cannot start an online match with a mouse, because the button is not there to press. OWNER: Netcode/UI (the lane that landed fafff19). The fix is small — \`pointer-events: none\` while hidden, or a \`#pr-connect-trace[hidden]{display:none}\` rule so the \`hidden\` attribute means what it says, and the panel should not outlive the lobby it was narrating.`,
  },
};

const manifest = JSON.parse(readFileSync(MANIFEST, 'utf8'));
for (const [id, patch] of Object.entries(entries)) {
  const i = manifest.findIndex((e) => e.id === id);
  const base = i >= 0 ? manifest[i] : { id, area: patch.area ?? 'online' };
  const next = { ...base, ...patch, id, area: patch.area ?? base.area ?? 'online', capturedAt: AT, buildSha: SHA };
  if (i >= 0) manifest[i] = next;
  else manifest.push(next);
  console.log(`${i >= 0 ? 'updated' : 'added  '} ${id} → ${next.verdict}`);
}
writeFileSync(MANIFEST, JSON.stringify(manifest, null, 2) + '\n');
console.log(`manifest: ${manifest.length} entries`);
