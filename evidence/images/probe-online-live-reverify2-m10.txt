FRESH LIVE WIRE PROBE — M10 online gates re-verify
capturedAt: 2026-07-29T09:36:11Z  (self-witnessed, repo HEAD 0f2479c)
target:     https://planet-rush-allocator.fly.dev
deploy:     server Fly/3d7232b6e (2026-07-27), health uptimeSeconds ~4200 (same running instance as the 09:33 probe)

RESULT: UNCHANGED. The fly-replay allocate defect is NOT fixed. All three online gates STAY FAILED.

======================================================================
[1] GET /health  ->  HTTP/2 200 application/json
    {"status":"ok","machines":2,"rooms":0,"reservations":0,"uptimeSeconds":4194}
    Fleet registered (machines:2), no rooms live.

[2] GET /machines ->  HTTP/2 200
    {"machines":[
      {"machine":"6836293b5161d8","region":"iad","capacity":32,"rooms":0,...},
      {"machine":"0800d5b6d62328","region":"iad","capacity":32,"rooms":0,...}]}
    Both gameservers heartbeating in iad, capacity 32 each.

[3] OPTIONS /rooms  (Origin: http://localhost:4173, ACRM POST)  ->  HTTP/2 204
    access-control-allow-origin: http://localhost:4173
    vary: Origin
    access-control-allow-methods: GET, POST, OPTIONS
    access-control-allow-headers: content-type
    access-control-max-age: 86400
    PREFLIGHT PASSES — allocator CORS layer is correct.

[4] POST /rooms  (Origin: http://localhost:4173, body {})  ->  HTTP/2 200
    content-type: text/plain
    via: 2 fly.io, 2 fly.io          <-- DOUBLE EDGE HOP = fly-replay
    (NO access-control-allow-origin header)
    body: "Planet Rush match server — connect a WebSocket to play."
    >>> THE DEFECT. The allocate is fly-replayed to a bare gameserver, whose
    >>> root text reply reaches the caller. No JSON, no room code, no ACAO.

[5] GET /health (immediately AFTER the POST /rooms attempt)  ->  HTTP/2 200
    {"status":"ok","machines":2,"rooms":0,"reservations":1,...}
    NEW DATA POINT THIS RUN: reservations went 0 -> 1. The allocator DID begin
    the allocate server-side (reserved a seat) — but rooms stays 0 and the
    HTTP body the client receives is the fly-replayed gameserver text, so the
    minted room+ticket JSON never reaches the browser. The allocate is
    server-started but client-invisible.

[6] CONTROL — POST /rooms/WXYZ/join (a NON-replayed allocator path)  ->  HTTP/2 404
    content-type: application/json
    access-control-allow-origin: http://localhost:4173
    body: {"error":"not-found"}
    Proves the allocator is directly reachable AND CORS-correct from a browser
    origin. ONLY the fly-replayed allocate (POST /rooms) path fails.
======================================================================

CONCLUSION: Blockers 1-3 (fleet registration, allocator CORS, client socket
handoff) remain fixed. The remaining deploy/routing defect — fly-replay applied
to POST /rooms redelivers the allocate to a gameserver that cannot mint+CORS a
room reply — is STILL PRESENT on the live deployment. A browser CREATE ROOM
therefore CORS-blocks the ACAO-less text/plain reply, fetch() throws, no code is
minted. Client front door confirms: red subtitle "Can't reach the servers.
Check your connection — PLAY SOLO still works." on BOTH desktop and portrait
phone (evidence/images/online-create-desktop.png, online-create-phone.png).

online-room-create   -> FAILED (no code minted from the browser)
online-two-clients   -> FAILED (no host code to share; guest path proven via 404)
online-reconnect     -> FAILED (no live match to drop from and rejoin)
