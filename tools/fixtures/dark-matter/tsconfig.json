{
  "//": "Fixture project for tests/tools/dark-matter-scan.test.ts. Deliberately outside the root tsconfig's `include` so the repo's own typecheck and its own dark-matter scan never see these files.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noEmit": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true
  },
  "include": ["src"]
}
