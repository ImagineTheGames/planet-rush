/**
 * src/bots/ — AI opponents. OWNER: Bot Engineer (GDD §3.3, §2.9).
 *
 * Hand-coded Easy/Medium/Hard behavior trees plus the seven bot personalities,
 * driving the same abstract `Action` interface a human uses (src/shared/types).
 * Fog-honest: bots perceive only what a human in their cockpit could. No LLM
 * calls at runtime — runtime token cost is zero.
 *
 * Placeholder only — no bot logic yet (day-0 scaffold).
 */
export const BOTS_PLACEHOLDER = true;
