/**
 * src/art/ — visuals and audio, generated as code. OWNER: Art & Audio Agent
 * (GDD §3.6, §5).
 *
 * Procedural SVG/sprite ships (one livery per bot personality), planets,
 * asteroids, wrecks and UI in the Cold Vacuum palette; the specified VFX set;
 * synthesized SFX (jsfxr-style) including the under-attack alarm; and an
 * ambient loop. Assets are code — reproducible, diffable, regenerable offline.
 *
 * Placeholder only — no assets yet (day-0 scaffold).
 */
export const ART_PLACEHOLDER = true;
