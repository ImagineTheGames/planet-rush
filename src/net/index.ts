/**
 * src/net/ — networking. OWNER: Netcode Engineer (GDD §3.5, §4.2).
 *
 * The `Transport` interface and both implementations (LocalLoopback for solo /
 * offline, WebSocketTransport for online), snapshot encoding, client-side
 * prediction and reconciliation, and the room/lobby protocol. The simulation
 * consumes ordered input ticks and never knows which transport it's talking to.
 *
 * Placeholder only — no protocol yet (day-0 scaffold; netcode lands mid-week).
 */
export const NET_PLACEHOLDER = true;
